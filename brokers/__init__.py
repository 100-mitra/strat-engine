"""Broker abstraction (Phase 3).

Reserved for the swappable ``Broker`` interface and adapters (paper, Alpaca sandbox). Paper/
sandbox only — never real orders. Implemented in Phase 3; kept as a package seam from the start so
the abstraction boundary is explicit.
"""
